{
  "name": "featureflow-node-sdk",
  "version": "0.7.0-beta.2",
  "description": "Featureflow sdk for Node",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=10.0.0"
  },
  "packageManager": "yarn@4.4.1",
  "files": [
    "dist",
    "!dist/**/*.map"
  ],
  "scripts": {
    "build": "babel src -d dist --source-maps && tsc",
    "watch": "babel --watch src -d dist --source-maps",
    "prepublish": "yarn build",
    "test": "node_modules/.bin/cucumber-js --require-module @babel/register -r features/step_definitions -r features/support --format=json:cucumber-report.json -t \"not @ignore and not @integration and not @builder-defers-implicit-attributes\" testbed/gherkin && node_modules/.bin/cucumber-junit < cucumber-report.json > output_JUnit.xml",
    "ci-test": "node_modules/.bin/cucumber-js --require-module @babel/register -r features/step_definitions -r features/support --format=json:cucumber-report.json -t \"not @ignore and not @integration and not @builder-defers-implicit-attributes\" testbed/gherkin && node_modules/.bin/cucumber-junit < cucumber-report.json > ~/junit.xml",
    "test:integration": "node_modules/.bin/cucumber-js --require-module @babel/register -r features/step_definitions -r features/support -t \"@integration\" features/integration.feature",
    "preexample:server": "yarn build",
    "example:server": "node examples/harness/server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/featureflow/featureflow-node-sdk.git"
  },
  "author": "Featureflow <featureflow@featureflow.io>",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/featureflow/featureflow-node-sdk/issues"
  },
  "homepage": "https://github.com/featureflow/featureflow-node-sdk#readme",
  "dependencies": {
    "big-integer": "^1.6.17",
    "debug": "^2.6.2",
    "request": "^2.88.0",
    "sha1-hex": "^1.0.0",
    "typescript": "^4.8.2"
  },
  "resolutions": {
    "form-data": "^2.5.6"
  },
  "devDependencies": {
    "@babel/cli": "^7.29.7",
    "@babel/core": "^7.29.7",
    "@babel/preset-env": "^7.29.7",
    "@babel/register": "^7.29.7",
    "chai": "^3.5.0",
    "cucumber": "^4.2.1",
    "cucumber-junit": "^1.7.1",
    "express": "^4.21.0",
    "moment": "^2.18.1"
  }
}
