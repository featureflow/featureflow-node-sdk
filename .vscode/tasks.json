{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "yarn: build (SDK)",
      "type": "shell",
      "command": "yarn build",
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "NODE_ENV": "development"
        }
      },
      "group": "build",
      "problemMatcher": []
    }
  ]
}
